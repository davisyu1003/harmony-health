{
  "name": "harmony-health",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix packages/web",
    "build": "npm run build --prefix packages/web",
    "test": "npm run test --prefix packages/web",
    "lint": "npm run lint --prefix packages/web",
    "install:all": "npm install --prefix packages/web && npm install --prefix packages/api"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "npm@10.9.0"
}
